<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nhimcoi.yuh.chopsticks">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAZinHY9NwELa9GHIQye05Q5Q5NMcujCV4" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/app_id" />

        <activity android:name=".View.SlashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Login_Signup.LoginActivity" />
        <activity android:name=".View.Home.HomePageActivity" />
        <activity android:name=".View.Login_Signup.SignUpActivity" />
        <activity android:name=".View.Home.DetailResActivity" />
        <activity android:name=".View.Home.DetailCommentActivity" />
        <activity android:name=".View.Home.GoogleMapsActivity"></activity>
    </application>

</manifest>